ec2 spot instance requests
  - can get a discount of up to 90% compared to On-demand
  - Define max spot price and get the instance while current spot price < max
    -the hourly spot price depends on offer and capacity
    - current spot price> your max price you can choose to stop or terminate your instance with a 2 minutes grace period
  -used for batch jobs, data analysis, or workloads

terminate spot instance:
1) create request
2) spot request
  2.1) maximum price
  2.2) desired number of instances
  2.3) launch specification
  2.4) request type: one-time & persistence
  2.5) valid from, valid until
3) request failed 
4) for persistence
  4.1) start
  4.2) stop 
  4.3) interupt
5) launch instances
spot fleets= set of sport instances + on-demand instances
spot fleets when you meet the target capacity with price constraints
spot instances:
  1) lowestPrice= from the pool with the lowest price
  2) diversified= distributed accross all pools
  3) capacityOptimized: pool with the optimal capacity for the number of instances
  4) priceCapacityOptimized: pools with highest capacity available, then select the pool with the lowest price


price type| price
On-demand  $0.10
Spot instance  0.038-0.039(up to 61% off)
reserved instance(1 year) 0.062(no upfront) - $0.058 (all upfront)
reserved instance(3 year) 0.043(no upfront) -0.037(all upfront)
ec2 savings plan(1 year) - 0.062-0.058
Reserved convertible instance(1 year) 0.071-0.066
dedicated host on-demand price
dedicated host reservation up to 70% off
capacity reservations on-demand price
